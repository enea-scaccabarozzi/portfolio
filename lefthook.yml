pre-commit:
  commands:
    biome:
      glob: "**/*.{ts,tsx,js,jsx,json,css,mjs}"
      run: bunx biome check --write {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}

pre-push:
  commands:
    build:
      run: bun run build
