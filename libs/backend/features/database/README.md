# backend-features-database

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-features-database` to build the library.

## Running unit tests

Run `nx test backend-features-database` to execute the unit tests via [Jest](https://jestjs.io).
