# backend-features-api

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-features-api` to build the library.

## Running unit tests

Run `nx test backend-features-api` to execute the unit tests via [Jest](https://jestjs.io).
